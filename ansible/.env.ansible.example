
# Ansible Provisioning Environment Variables
# Copy this file to .env.ansible and fill in your values
# Usage: cp .env.ansible.example .env.ansible

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# Domain Configuration
DOMAIN_NAME=example.com
SSL_EMAIL=admin@example.com
ANSIBLE_REMOTE_USER=root

# Database Configuration
DB_PASSWORD=your_secure_database_password_here

# GitHub Actions SSH Key (for automated deployments)
# Generate: ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy -N ""
# Then: export GITHUB_ACTIONS_SSH_KEY=$(cat ~/.ssh/github_actions_deploy.pub)
GITHUB_ACTIONS_SSH_KEY=ssh-ed25519 AAAAC3Nza... your_public_key_here

# ===========================================
# OPTIONAL VARIABLES (for app deployment)
# ===========================================

# Git Repository (required only if deploying app during provisioning)
# GIT_REPO_URL=git@github.com:your-username/your-repo.git
# GIT_BRANCH=main

# Django Admin User (for initial superuser creation)
# DJANGO_ADMIN_USER=admin
# DJANGO_ADMIN_PASSWORD=your_secure_admin_password
# DJANGO_ADMIN_EMAIL=admin@example.com

# ===========================================
# CLOUD PROVIDER SPECIFIC
# ===========================================

# AWS Configuration (for aws_security role)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret

# DigitalOcean Spaces Configuration (for backups)
# DO_SPACES_KEY=your_spaces_key
# DO_SPACES_SECRET=your_spaces_secret
# DO_SPACES_REGION=nyc3

# ===========================================
# BACKUP CONFIGURATION
# ===========================================

# Backup bucket name (defaults to: {project_name}-backups-{cloud_provider})
# BACKUP_BUCKET=my-custom-backup-bucket

# ===========================================
# MONITORING CONFIGURATION
# ===========================================

# Promtail/Loki Configuration (optional)
# PROMTAIL_LOKI_URL=http://your-loki-instance:3100/loki/api/v1/push

# ===========================================
# ADVANCED OPTIONS
# ===========================================

# Skip certain roles during provisioning
# setup_github_actions=false
# enable_monitoring=false
# deploy_app=false

# Auto-reboot after kernel updates
# auto_reboot=false