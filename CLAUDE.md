# CLAUDE.md

Project instructions live in a single source, `AGENTS.md`, shared with other agents. Read it:

@AGENTS.md

Claude Code auto-loads the project skills from `.claude/skills/` (relative symlinks into the canonical `.agents/skills/`). Invoke the skill matching the task before starting; edit skills only in `.agents/skills/`.
