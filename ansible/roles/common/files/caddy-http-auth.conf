# Fail2Ban filter for Caddy HTTP authentication failures
# Matches 401 Unauthorized and 403 Forbidden responses

[Definition]

# Match 401/403 status codes in Caddy logs
failregex = ^<HOST> .* "(GET|POST|HEAD|PUT|DELETE|PATCH|OPTIONS).*" (401|403) .*$

ignoreregex =