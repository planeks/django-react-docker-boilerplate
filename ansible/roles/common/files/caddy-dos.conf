# Fail2Ban filter for Caddy DoS/DDoS attacks
# Detects potential denial of service attacks through rapid requests

[Definition]

# Match any request (will be triggered by frequency)
failregex = ^<HOST> .*$

ignoreregex =